[build]
  publish = "outputs/gsil-netlify-static"
  command = "echo 'GSIL static frontend ready'"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
